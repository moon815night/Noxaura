# 项目开发规范（Aider 每次对话都会自动读取本文件，必须严格遵守）

本文件的作用：约束 AI 助手的做事方式，防止"改一处坏一处"和"代码越堆越长"。

---

## 一、文件结构：按类型分开放（最重要）

随着项目变大，**禁止把所有代码塞进一个 html 文件**。必须遵守以下结构：

```
index.html            只写 HTML 结构，不写大段 <style> 和 <script>
css/
    base.css          全局重置、变量、字体
    layout.css        页面布局、容器、栏位
    components.css    按钮、气泡、头像等组件
    theme.css         主题配色（颜色变量集中在这里）
    <模块名>.css      每个功能模块一个文件
js/
    main.js           入口：初始化、串联各模块
    state.js          数据、状态管理
    ui.js             界面渲染、DOM 操作
    api.js            网络请求（如果以后要接后端）
    <模块名>.js       每个功能模块一个文件
assets/
    images/           图片
    icons/            图标（svg）
    其他静态资源
```

**硬性规则：**
1. `index.html` 里用 `<link rel="stylesheet" href="css/xxx.css">` 引入样式，
   用**普通脚本 + defer** 按顺序引入 js，例如：

   ```html
   <script src="js/ui.js" defer></script>
   <script src="js/main.js" defer></script>
   ```

   ⚠️ **严禁使用 `<script type="module">`，也严禁在 js 里写 `import` / `export`。**
   原因：`type="module"` 的脚本在**双击 index.html 直接打开时**（file:// 协议）
   会被浏览器安全策略拦截，导致整页 JS 一行都不执行——
   表现就是「按钮全部失灵、气泡框画不出来」。这个坑本项目已经踩过一次。
   多个 js 文件之间用**全局函数**互相调用，靠 defer 保证执行顺序（先依赖、后入口）。
2. **每个文件不超过 300 行**。超过就按功能拆成新文件。
3. 新增功能时，**新建独立文件**，而不是往已有文件里继续堆。
4. 颜色、字号、圆角、间距等**统一写成 CSS 变量**，放在 `css/theme.css` 的 `:root {}` 里；其他地方一律用 `var(--xxx)`，不要写死颜色值。
5. 中文文件名一律不用（除说明文档），避免编码问题。

## 二、动手前必须做的事

1. **改动超过 1 个文件时**，先用一句话列出「要改哪些文件、各改什么」，等我确认后再动手。
2. **一次只做一件事**。我如果一次提了多个需求，先做完最重要的那个，做完告诉我，再问我要不要继续下一个。
3. **不确定的地方先问我**，不要自己猜着做。

## 三、禁止事项

1. **不要创建我没有要求的文件**。
2. **不要把我说的话、贴给你的说明文字当成"内容"保存成文件**。
   只有我明确说"新建一个文件叫 xxx"时才创建文件。
3. **不要删除、重写与本次需求无关的代码**。改 A 功能就只碰 A 相关的代码。
4. **不要擅自改变整体设计风格**（配色、字体、布局方向）除非我明确要求。
5. **不要引入外部网络依赖**（CDN 上的 js/css/字体/图标库）。
   本项目要求离线可用，所有资源必须放在本地或内联。
6. **不要写"假的"功能**。做按钮就让它真的能点、真的有效果；
   如果暂时只能做视觉占位，必须明确告诉我"这个按钮目前只是样子，还不能用"。
7. **不要使用 ES Module 语法**：不写 `import` / `export`，不写 `<script type="module">`。
   拆分 js 文件时，用全局函数互相调用 + 普通 `<script src="..." defer>` 按顺序引入。
8. **不要把 HTML 里写死的 `d` 路径当成气泡/图形的真实尺寸**。
   依赖 JS 计算的图形（如气泡外框），HTML 里给一个空的合法值即可，
   不要写 `L 100% 0` 这种非法路径（SVG 的 `d` 不支持百分比，会导致图形残缺）。

## 四、做完之后必须说明

每次改完代码，用中文简短告诉我：
1. 改了哪些文件
2. 每个文件改了什么
3. 我该怎么验证（点击哪里、看什么效果）
4. 有没有还没做、需要我决定的地方

## 五、回复语言

一律用**中文**回复我。不要用英文术语堆砌，要说人话。
