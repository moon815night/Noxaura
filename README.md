# Noxaura

Noxaura 项目仓库。

## 本地开发

代码在本地 `chat web` 目录中编写，使用 [Aider](https://aider.chat/) 作为 AI 结对编程助手。

## 提交与推送

在项目目录（`chat web`）中执行，或直接双击桌面上的「推送到 GitHub」：

```
git add -A
git commit -m "说明这次改了什么"
git push
```

首次推送会弹出窗口要求登录 GitHub，登录一次之后会自动记住，以后推送不再需要登录。
